{
  "name": "absher-neuron-database",
  "version": "2.0.0",
  "private": true,
  "description": "Absher Neuron 2.0 — PostgreSQL data layer (Prisma)",
  "author": "Abdulaziz AlAmawi",
  "license": "MIT",
  "prisma": {
    "schema": "prisma/schema.prisma",
    "seed": "node --loader ts-node/esm prisma/seed.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "deploy": "prisma migrate deploy",
    "seed": "prisma db seed",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  },
  "devDependencies": {
    "prisma": "^6.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
